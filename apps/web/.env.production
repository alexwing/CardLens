# Imagenes de las cartas servidas desde el CDN propio (self-hosting).
# Vite carga este fichero en los builds de produccion (npm run build), que es
# lo que empaqueta el escritorio y la futura app Android.
VITE_IMAGE_BASE=https://cardlens.mappuzzle.xyz/catalog
